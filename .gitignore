# Secrets and runtime data — never commit
.env
.bootstrap-secret
*.db
*.db-journal

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
.venv/
venv/

# Stray binaries from earlier deployments (Caddy etc.) — not part of the project.
caddy.exe
caddy

# Editors / IDEs
.idea/
.vscode/
*.swp

# OS junk
.DS_Store
Thumbs.db

# Claude Code session scratch (worktrees, agent state)
.claude/
